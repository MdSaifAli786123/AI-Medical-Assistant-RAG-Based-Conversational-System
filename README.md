# AI Medical Assistant – RAG-Based Conversational System

This project is no code project on n8n and vectorshift so only html code is available to display
For deployment
change file name to index.html->Settings->Pages->select none->main branch->done
<br>
#only work in public library
